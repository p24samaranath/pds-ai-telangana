# ── Slim requirements for cloud deployment (Render free tier ~512 MB RAM) ──
# Full ML stack is in requirements.txt for local dev.
# For production, torch CPU-only + no prophet keeps the image < 400 MB.

# Core Framework
fastapi>=0.104.1
uvicorn[standard]>=0.24.0
python-dotenv>=1.0.0
pydantic>=2.5.0
pydantic-settings>=2.1.0

# Database
sqlalchemy>=2.0.23
aiosqlite>=0.19.0

# AI / LLM
anthropic>=0.18.1
google-genai>=1.0.0          # Google Gemini fallback (use when GEMINI_API_KEY is set)

# ML & Data Science — CPU-only torch (no CUDA, ~200 MB vs ~700 MB)
--extra-index-url https://download.pytorch.org/whl/cpu
torch==2.2.2+cpu
numpy>=1.26.2
pandas>=2.1.2
scikit-learn>=1.3.2
scipy>=1.11.4

# Geospatial (pure-Python, no GDAL)
shapely>=2.0.2

# Async / HTTP
httpx>=0.25.2

# Utilities
python-multipart>=0.0.6
aiofiles>=23.2.1
